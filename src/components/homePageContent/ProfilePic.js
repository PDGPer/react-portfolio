import { Portrait } from "../../imgs/svgLibrary";

export const ProfilePic = () => {
  return <Portrait />;
};

export const profilePicString =
  "<div class='code-container'>" +
  "<pre>" +
  "<code class='code'>" +
  "import { Portrait } from 'svgLibrary'" +
  "<br><br>" +
  "const ProfilePic = () =〉 {" +
  "<br>&nbsp;&nbsp;" +
  "return 〈Portrait /〉" +
  "<br>" +
  "}" +
  "</code>" +
  "</pre>" +
  "</div>";
